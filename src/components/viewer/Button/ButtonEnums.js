const type = {
  primary: 'primary',
  secondary: 'secondary',
  default: 'none',
};

const size = {
  medium: 'medium',
  small: 'small',
};

const orientation = {
  horizontal: 'horizontal',
  vertical: 'vertical',
};

export { type, size, orientation };
